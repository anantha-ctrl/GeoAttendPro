# Base URL of the PHP API front controller (public/index.php).
# Leave empty to auto-detect from browser hostname (works with localhost + LAN IP).
# Override only if needed: VITE_API_BASE_URL=http://localhost/GeoAttendPro/backend/public
